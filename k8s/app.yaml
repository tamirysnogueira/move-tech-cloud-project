apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloud-project
  labels:
    app: cloud-project
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cloud-project
  template:
    metadata:
      labels:
        app: cloud-project
    spec:
      imagePullSecrets:
      - name: registry-secret
      containers:
      - name: cloud-project
        image: container-registry.br-se1.magalu.cloud/${MGC_REGISTRY_NAME}/cloud-project:latest
        ports:
        - containerPort: 8000
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: db-secret
              key: url


---

apiVersion: v1
kind: Service
metadata:
  name: cloud-project
  labels:
    app: cloud-project
spec:
  type: LoadBalancer
  selector:
    app: cloud-project
  ports:
  - name: http
    port: 80
    targetPort: 8000
    protocol: TCP
